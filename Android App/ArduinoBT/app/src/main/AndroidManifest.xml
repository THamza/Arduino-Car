<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.car_control.led">

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.car_control.car.SplashScreen"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.car_control.car.DeviceList"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name="com.car_control.car.carControl"
            android:label="Car Control">

        </activity>
        <activity
            android:name="com.car_control.car.AboutActivity"
            android:label="@string/title_activity_about">
        </activity>

    </application>

</manifest>
